- name: Install desktop environment
  block:
    - name: Install KDE
      include_tasks: kde.yml
      when: desktop_env_choice == 'kde'
    - name: Install Hyprland
      include_tasks: hyprland.yml
      when: desktop_env_choice != 'kde'

